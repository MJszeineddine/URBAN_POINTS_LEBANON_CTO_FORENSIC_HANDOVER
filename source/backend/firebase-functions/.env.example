# =============================================================================
# URBAN POINTS LEBANON - FIREBASE FUNCTIONS ENVIRONMENT VARIABLES
# =============================================================================
# This file contains ALL environment variables required for the backend.
# Copy this to .env and fill in actual values (NEVER commit .env to git!)
#
# For production deployment, set these via Firebase Console or gcloud CLI
# =============================================================================

# -----------------------------------------------------------------------------
# CORE SECURITY
# -----------------------------------------------------------------------------
# QR Token Secret - Used for secure QR code generation and validation
# CRITICAL: Generate a strong random 64-character hex string for production
# Command: openssl rand -hex 32
QR_TOKEN_SECRET=REPLACE_WITH_STRONG_SECRET_IN_PRODUCTION

# -----------------------------------------------------------------------------
# PAYMENT GATEWAYS
# -----------------------------------------------------------------------------
# OMT (Lebanese Payment Provider)
OMT_API_KEY=PLACEHOLDER
OMT_WEBHOOK_SECRET=omt-secret
OMT_MERCHANT_ID=PLACEHOLDER

# Whish Money (Lebanese Payment Provider)
WHISH_API_KEY=PLACEHOLDER
WHISH_WEBHOOK_SECRET=whish-secret
WHISH_MERCHANT_ID=PLACEHOLDER

# Card Payment Gateway
CARD_WEBHOOK_SECRET=card-secret

# Stripe (International Payments) - DEFERRED FEATURE
# STRIPE_ENABLED: Set to "1" to enable Stripe, "0" to disable (default)
# When disabled: All Stripe code paths are skipped at runtime
# When enabled: Requires sk_live_* (production) or sk_test_* (test only) keys
STRIPE_ENABLED=0
STRIPE_SECRET_KEY=PLACEHOLDER
STRIPE_WEBHOOK_SECRET=PLACEHOLDER

# -----------------------------------------------------------------------------
# SMS NOTIFICATIONS
# -----------------------------------------------------------------------------
# Twilio SMS Gateway
TWILIO_ACCOUNT_SID=PLACEHOLDER
TWILIO_AUTH_TOKEN=PLACEHOLDER
TWILIO_PHONE_NUMBER=PLACEHOLDER

# Alternative SMS Provider API
SMS_API_KEY=PLACEHOLDER

# -----------------------------------------------------------------------------
# MONITORING & ALERTS
# -----------------------------------------------------------------------------
# Slack Webhook for Error Notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/PLACEHOLDER

# -----------------------------------------------------------------------------
# FIREBASE CONFIGURATION
# -----------------------------------------------------------------------------
# These are automatically set by Firebase Functions runtime
# FIREBASE_CONFIG=(auto-set by Firebase)
# GCLOUD_PROJECT=(auto-set by Firebase)
# FUNCTIONS_EMULATOR=(auto-set when running locally)
# FIRESTORE_EMULATOR_HOST=(auto-set when using emulator)

# -----------------------------------------------------------------------------
# DEVELOPMENT / TESTING
# -----------------------------------------------------------------------------
# Set to 'true' when running in Firebase Emulator
# FUNCTIONS_EMULATOR=true
# FIRESTORE_EMULATOR_HOST=localhost:8080

# =============================================================================
# DEPLOYMENT INSTRUCTIONS
# =============================================================================
# 
# For Firebase Functions v2 (recommended method):
# 
# 1. Via Firebase Console:
#    - Go to: https://console.firebase.google.com/project/YOUR_PROJECT/functions/config
#    - Add each environment variable as a secret or config value
#
# 2. Via gcloud CLI:
#    gcloud functions deploy FUNCTION_NAME \
#      --set-env-vars QR_TOKEN_SECRET=your_secret_here,OMT_API_KEY=your_key_here
#
# 3. Via .env file (for local development only):
#    - Copy this file to .env
#    - Fill in actual values
#    - NEVER commit .env to version control
#
# =============================================================================
